## Learned User Preferences

- **Default language for Roman (assistant + user-facing repo edits):** **Russian** for chat replies and for instructions or documentation meant for Roman to read in this workspace, unless he explicitly asks for another language. **Do not** use Ukrainian in assistant messages to Roman or in mixed setup prose for him unless he requests Ukrainian.
- **Surname in Russian/Ukrainian:** **Мацукатов** (canonical Cyrillic). Avoid mistaken spellings like **Матсукатов**. English contexts may use **Matsukatov**. Full rule: `.cursor/rules/dex-user-name-spelling.mdc`.
- **Repository root:** Do not add loose files or secrets at the Dex repo root; use `Credentials/`, PARA folders, `.scripts/`, etc. See `.cursor/rules/dex-root-hygiene.mdc` and `Credentials/README.md`.
- **No opaque abbreviations in user-facing text** (заметки vault, чеклисты, Linear, письма): не использовать сокращения без смысла для читателя, например `компы` вместо «денежные компенсации», или `DD` без пояснения. Вместо «DD» писать по-русски, например **проверка инвесторами перед сделкой** (в англоязычных материалах это иногда называют due diligence). Если аббревиатура всё же нужна, один раз в том же артефакте дать расшифровку обычными словами. Исключения: идентификаторы задач (INA-28), коды трекинга (BND-HR-001), дословные имена файлов и папок из исходной системы.
- **Apple Health export lookup:** Follow `.cursor/rules/apple-health-export-routing.mdc` as the canonical routing rule.
- **Cursor chat log in Dex:** Follow `.cursor/rules/dex-chat-session-log.mdc` for enforcement and `System/Chat_logs/README.md` for human-facing behavior notes.
- **First reply bootstrap (hard requirement):** Full canonical contract, recovery, and proof requirements live in `.cursor/rules/session-bootstrap-enforcer.mdc`. Keep `.cursorrules` only as the minimal fallback reminder.
- **Superpowers + Karpathy after turn one:** Follow `.cursor/rules/dex-coding-skills-gate.mdc` as the canonical paired re-anchor rule for substantive coding turns.
- **Cursor Plan mode:** The repo cannot force the IDE into Plan mode (no hook API). Use **Shift+Tab** in the chat input or the mode picker; Cursor may **suggest** Plan for complex tasks ([docs](https://cursor.com/docs/agent/plan-mode)). Habit for big features: Plan first → review → Build.
- **Execute, don’t delegate shell work:** Follow `.cursor/rules/action-first-outcomes.mdc` as the canonical rule; keep the behavior unchanged.
- **Failed tests (unit/e2e/live):** Do not leave FAIL without a fix attempt. Either fix the implementation or fix the test, then re-run. Canonical: `.cursor/rules/dex-failed-test-remediation.mdc`.
- **Job-search timing:** Full-flow and block-live (Skills) use atomic per-step/gate budgets, `[@flow-step]` / `[@gate]` markers, progress JSON, watchdog exit-on-child-done. `npm run job-search:full-flow-total-ms` / `job-search:block-live-total-ms` for shell ceiling. See `.cursor/rules/job-search-teal-timeouts.mdc`.
- **Banda Google Drive (Board + Presentations bundle + External Audits):** **Only content** belongs on those Drive trees (office files, investor-facing markdown, media as defined per flow). **Do not** upload or leave **system** artifacts there: repo README/how-the-script-works docs, `.obsidian/`, `.cursor/`, `package.json`, processing-only notes at Drive roots, etc. Canonical: `.cursor/rules/banda-google-drive-content-only.mdc`. After any **Obsidian structure or `Obsidian/**/*.md` content** change under **Board**, **Presentations Bundle**, or **External Audits**, **same turn** run the matching **full** Drive sync **without** `--dry-run` (see `.cursor/rules/banda-obsidian-structure-drive-sync.mdc`): e.g. `npm run banda:board-drive-sync`, `npm run banda:presentations-bundle-drive-sync` (or flags from that rule), `python3 .scripts/banda/external_audits_drive_upload.py`. **Do not** treat **`--dry-run` only** as done work for those changes; use `:dry-run` scripts only when the user explicitly wants a no-write preview or you are debugging API errors. If credentials are missing, say so in one short block instead of substituting dry-run for upload. For **`2. Banda Presentations Bundle`**, Drive is still the **source of truth for cloud-facing content**; before sync or Drive advice, **read** that bundle’s `README.md` for folder names and IDs; **local child folder names under the bundle root must match Drive** (same three «… (Drafts)» content trees); extra technical files may exist only at the bundle root as documented there. Default bundle sync **uploads** content notes under **`Obsidian/`** to Drive; it **does not** push vault meta unless legacy `--upload-vault-meta-to-drive`. The same sync pass **trashes** known stale Drive layout: duplicate `*.md` next to an `Obsidian/` folder when the same basename exists inside `Obsidian/`, and a **bundle-root** `Obsidian/` folder on Drive when that folder is absent locally (see report keys `drive_duplicate_root_md_trashed`, `drive_stale_root_obsidian_folder_trashed`). **External Audits** upload uses the same cleanup before file writes. For **media without docx/xlsx**, use `npm run banda:presentations-bundle-drive-sync:media-only` or `--skip-docx-xlsx`. Also: `.cursor/rules/banda-presentations-drive-source-of-truth.mdc`.
- **Parimatch Test Assignment (`04-Projects/Parimatch_Test_Assignment/final/`):** After any edit to submission/scope/strategy/analysis markdown that feeds Google Docs, **always** run `python3 04-Projects/Parimatch_Test_Assignment/gdrive_upload.py` from the Dex repo root (no “if you want” prompt), then update the `for doc_id in [...]` list in `gdrive_upload.py` with the new document IDs from the script output.
- **Google account confirmation is mandatory before any write operation:** Before creating/saving/uploading anything to Google services (Google Docs, Google Sheets/Spreadsheets, Google Drive files/folders, Google Slides, or any Google artifact), always ask the user which account to use (`personal` or `work`) unless the user explicitly specified it in the current request. For execution, first verify active OAuth identity via MCP account check (or API equivalent) and refuse write if account does not match the user-selected target.
- **Google OAuth tokens (hard):** Never delete, move, or overwrite `Credentials/personal/*.json` or `Credentials/google-work/*.json` to fix auth. Personal and work/Banda use **separate** token files and MCP env paths (`google-*-mcp` vs `google-*-work-mcp`). Re-auth only the missing file in its folder; do not touch the other account's token.
- **Plain language for every chat reply:** lead with meaning for the user (full sentences); do not open with jargon dumps, code identifiers, or raw `[[wikilink]]`/path chains unless the user asked for that. Applies to **all** topics, not only vault or Obsidian. Canonical: `.cursor/rules/dex-plain-language-structure.mdc` and `CLAUDE.md` USER_EXTENSIONS «Формат ответов в чате» points 7–12 (system behavior: 7–8; universal rule including vault: 9; адресные инструкции: 10; дубли и скобки: 11; чистовики: 12).
- **Instructions to Roman must be step-by-step:** numbered actions, UI path (menu → button), terminal command with full path, and what success looks like per step. No compressed «fill token with permissions X then run script» blocks. Canonical: `.cursor/rules/dex-step-by-step-instructions.mdc`.
- **No tautological parentheses or duplicate «clarifications»:** do not restate the same claim in brackets or a second paragraph unless you add a new number, condition, or explicit axis; do not let a parenthetical read as confirming the prior sentence while actually mixing a different metric or contradicting it. Canonical: `.cursor/rules/dex-plain-language-structure.mdc` (§6) and `CLAUDE.md` USER_EXTENSIONS point 11.
- **Clean deliverable MD:** no meta about vault markdown rules, repo policy, Cursor, or «this subsection adds…» inside final submissions, test answers, customer PRDs, or other external-facing prose in `.md`. Canonical: `.cursor/rules/dex-plain-language-structure.mdc` (§7) and `CLAUDE.md` USER_EXTENSIONS point 12.
- **Instructions must read as yours, not generic:** Before numbered steps, tie to what Roman just said and state the goal on his machine (Mac, DEX repo, Cursor). Each major step includes what he should see if it worked. See пункт 5 in `.cursor/rules/dex-plain-language-structure.mdc`.
- When describing instructions for Cursor or Claude Code, use a structured format (numbered list or bullets), not a long paragraph.
- **Chat replies to Roman:** prefer bullets or numbered lists over long dense paragraphs; one short lead sentence with meaning, then lists for steps or options.
- **Cognitive load (three layers):** Canonical `.cursor/rules/dex-cognitive-load-budget.mdc` — (1) вердикт, (2) действия, (3) детали. **Chat eval (mandatory):** `.cursor/rules/dex-chat-response-eval.mdc` + `npm run dex:eval-chat-response`; agent self-check before send; `stop` hook + `System/logs/chat-response-eval.jsonl` for auto-rewrite. Meta-commands: `вердикт` / `действия` / `разверни` / `озвучь`.
- **A/B experiment updates:** Lead with **relative lift %** and control → variant rates in chat/Slack; not absolute pp alone in layers 1–2 — `.cursor/rules/experiment-updates-relative-lift.mdc`.
- **A/B experiment conclusions (Ruby Labs):** Do not recommend close/ship without 2–3 stable full days, purchase guardrails check, and Ihor approval before prod — `.cursor/rules/vegas-bonanza-experiment-readout-conclusion.mdc`.
- **Mixpanel EXP boards:** Every experiment board — **Relative uplifts** (vs **control**) + **Stat. sig.** on all metrics — `04-Projects/Vegas_Bonanza/Mixpanel_Experiment_Board_Standard.md`.
- **Vegas Bonanza experiment rollout (GrowthBook):** Never stop/start/temporary-rollout or ship winner to 100% via API/scripts without **explicit Roman confirmation in chat**; ship decisions require **Ihor Herasymov** approval first. Analysis and drafts only by default — `.cursor/rules/vegas-bonanza-experiment-rollout-approval.mdc`.
- **Chat file paths:** When citing repo files in chat to Roman, use **full relative path from DEX root** (Cmd+P), not basename only — `.cursor/rules/dex-chat-file-paths.mdc`; eval check `file_refs_full_relative_path`.
- **Applicator from Dex root (`04-Projects/Applicator/**`):** Follow `.cursor/rules/applicator-development-git.mdc` — отдельный git в папке Applicator; после каждого логического изменения **коммит** агентом (push только по явной просьбе). **После коммита** — **автоматический deploy на staging** (`deploy-staging-api.sh` / `deploy-staging-pages.sh`); **запрещено** просить Roman «задеплой» / «обнови staging». Перебивает общее «коммит только по просьбе» для путей Applicator.
- **Ultradian + agent contract:** Dedicated Google Calendar **`DEX-RHYTHM`** (not personal primary); full grid 08:00–23:00 via `npm run cognitive:ultradian-calendar-sync`; reconcile `npm run cognitive:ultradian-reconcile`. See `05-Areas/Cognitive_Performance/README-ultradian.md`.
- In 1% Better / Substack day posts: put "Day X of 30" first, then title, then subtitle.
- In articles: do not use raw technical config (e.g. MCP JSON); describe user-facing steps (prompting, UI) so users get the outcome without editing config.
- Cursor: path to MCP is Settings -> Cursor Settings -> Tools and MCP (not "Settings -> MCP").
- **MCP health check in chat to Roman:** After the MCP health script, report in **plain Russian** first: **local Cursor tool connections** from Tools and MCP (not «stdio», not exit codes as the headline). **Preferred wording:** success — «Все подключения MCP инструментов в порядке. В данном чате можно ими пользоваться.»; one failure — «Не поднимается подключение к … (имя из списка) MCP, в настройках Tools and MCP посмотри статус / перезапусти Cursor». **Do not** glue a long parenthesis to those lines (stdio / repo root / URL list); URL-only entries (e.g. Atlassian) — **separate** short sentence without «stdio». Codes and raw JSON only after the human summary or if Roman explicitly asks for debug detail. Canonical: `CLAUDE.md` USER_EXTENSIONS «Отчёт о проверке MCP в чате» and `.cursor/rules/dex-plain-language-structure.mdc`.
- **Paperclip agents (HIR / Applicator):** In `board-read`, Roman-facing comments, and work-product `displayPath`, use Dex vault paths (`04-Projects/Applicator/...`), not workspace-only `docs/...`. All comments Roman may see in «Вся переписка» — Russian prose per `04-Projects/Applicator/instructions/ROMAN_FACING_VOICE.md` (no `Was:`/`Now:` telegraph); board-read uses format A, renames/status use format B. **Принципы понятного ответа** (§ в том же файле): ответ первым; lab ≠ prod ≠ Approve — отдельные блоки; жаргон и ярлыки с русской расшифровкой; **в chat дублируй шаги inline** (не «смотри board-read»); один вопрос Roman → один ответ за run. Отдельный doc — дополнительно, переписка самодостаточна.
- Atlassian MCP: authorization can drop after weeks; re-authorize via Settings -> Cursor Settings -> Tools and MCP -> Connect to fix.
- Claude Code Connectors: Settings -> Connectors -> Browse Connectors, find the server, click plus to add.
- LinkedIn post when promoting an article: state problem and solution briefly; link to the full post for details; do not rewrite the article in the post body.
- When asked for a LinkedIn post, produce a single post, not carousel or multi-slide text.
- SafeNSafe / product architecture docs (e.g. `04-Projects/Only_Stories_Adult/SafeNSafe/`): do not add parenthetical расшифровки (e.g. “same as US competitive comparison”, “ReelShort pattern”, “aligned with competitor mix”). State the fact once; no extra gloss in parentheses.
- **Job-search full flow — Chrome без переключения фокуса:** Шаги 1–4 (Dex extension / LinkedIn) и 6–10 (Teal Playwright) не должны выводить Chrome на frontmost. Агент **сам** поддерживает в `.env` и `env.example` строку `TEAL_CHROME_LAUNCH_MODE=pipe_minimized` (если её нет — добавить). Профили — **пул Teal** (`teal/.chrome-profile*`, `acquireTealProfile`), не фиксированный `Default` / `Profile 1`. **Не** просить Roman вручную добавлять `DEX_CHROME_PROFILE_DIRECTORY`. Канон: `.claude/reference/teal-chrome-launch-modes.md`, `.scripts/job-search/dex-chrome-open-background.cjs`. Legacy foreground: `DEX_CHROME_ALLOW_FOCUS_STEAL=1` или `DEX_LINKEDIN_USE_OPEN=1`. Dex Load unpacked — один раз в каждом pool-профиле, где нужен capture.

## Learned Workspace Facts

- Linear: API key is under Settings -> API -> Member API keys (or Security & access for personal keys); do not refer to "Personal API Keys" as a section name.
- Booking Cars (Dex extension): when the user asks to run or open capture with **their logged-in Booking session**, use **`npm run booking-cars:open -- "<url>"`** (opens the user’s Google Chrome profile and appends **`dex-booking-autostart=1`** so capture auto-starts). Do **not** use **`npm run booking-cars:capture-run`** for that (Playwright uses a separate empty profile). See `dex-linkedin-extension/README.md` (Booking section).
- **Whisper `large-v3` (faster-whisper) — предзагрузка с прогрессом в терминале:** Вызов **`download-whisper-weights large-v3`** (и **`python3 packages/transcript-skill/scripts/download_whisper_weights.py`**) **не показывает** полос прогресса: в **`faster_whisper.utils.download_model`** в **`snapshot_download`** передаётся **`tqdm_class=disabled_tqdm`**. Чтобы **видеть прогресс**, скачивать в кэш Hugging Face (тот же, что использует **`faster-whisper`**) командой **`hf`** из пакета **`huggingface_hub`:** `HF_HUB_DISABLE_PROGRESS_BARS=0 hf download Systran/faster-whisper-large-v3 --no-quiet` (репозиторий совпадает с картой моделей в **`faster_whisper`**). Для полной перекачки при необходимости: **`--force-download`**. Если пользователь просит «скачать с прогрессом» — **не** предлагать только **`download-whisper-weights`** без этого уточнения; по умолчанию для предзагрузки с UI прогресса использовать **`hf download`** как выше.
