# Операторский контекст (перенесён)

Весь текст этого раздела перенесён в **`chat-project-rules.md`**, заголовок в том файле: **«Операторский контекст (Match Club, США)»**.

Сервер подмешивает в системный промпт **один** блок правил из `chat-project-rules.md` (без отдельного второго блока из этого файла).

Эндпоинт `GET /api/chat-operator-workflow` по-прежнему отдаёт содержимое **этого** файла (краткая отсылка), чтобы старые ссылки не ломались.
