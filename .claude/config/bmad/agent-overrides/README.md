# Agent overrides (optional)

Put project-specific BMAD agent template overrides here if you use the BMAD orchestrator and override `agent_overrides_folder` in `bmad/config.yaml` to point to this path.

Default upstream convention was `bmad/agent-overrides/` at repo root; in this vault we keep overrides next to other BMAD config under `.claude/config/bmad/`.
