{
  "name": "user-telegram",
  "description": "Telegram: list chats, chat folders (e.g. Recruiting), get messages, search (user account)",
  "server": {
    "command": "bash",
    "args": ["core/mcp/run-telegram-mcp.sh"],
    "env": {
      "VAULT_PATH": "${workspaceFolder}",
      "TELEGRAM_SESSION_PATH": "${workspaceFolder}/.claude/telegram/telegram"
    }
  },
  "notes": "Uses uv to install Telethon+mcp on the fly. One-time login: TELEGRAM_API_ID and TELEGRAM_API_HASH from my.telegram.org, then npm run job-search:telegram-login"
}
